<?php
/**
 * Slot by default : contenu additionnel of <head>.
 * required tags (meta, title, app.css, theme CSS variables)
 * are injected by layout_top.php before this slot — a theme ne can therefore * do not remove it, only add to it.
 */
?>
